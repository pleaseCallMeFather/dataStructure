队列也是一种线性结构
相比数组，队列对应的操作是数组的子集
只能从一端（队尾）添加元素，只能从另一端（队首）取出元素
举例：和生活中的排队一样 First in First out

循环队列之所以要浪费一个空间是因为只有（tail + 1）% capacity = front时才能说明还剩一个空间就要装满了需要进行扩容
如果把这个空间用了还怎么判断队列有没有装满？
      f
7 8 X 3 4 5 6
  t

